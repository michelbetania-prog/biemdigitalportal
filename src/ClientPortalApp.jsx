import { createElement, Fragment, useEffect, useState } from './mini-react.js'
import {
  AlertTriangle, ArrowLeft, ArrowRight, Bell, CalendarDays, CheckCircle2, Clock3,
  Briefcase, CreditCard, FileText, Grid2X2, Image, LayoutDashboard, LogOut, Menu, MessageCircle,
  Package, Palette, Plus, Receipt, Send, Sparkles, Video, X,
} from './icons.jsx'
import {
  createClientRequest, loadPortalWorkspace, requestEventReschedule,
  reviewClientDeliverable, saveBrandBasics, saveNotificationPreferences, uploadBrandLogo,
} from './lib/portal-api.js'
import GuideExample from './GuideExample.jsx'

const empty={account:null,brand:null,events:[],deliverables:[],invoices:[],requests:[],tasks:[],resources:[],services:[],preferences:null}
const labels={pending:'Pendiente',paid:'Pagada',overdue:'Vencida',approved:'Aprobado',published:'Publicado',client_review:'Por revisar',changes_requested:'Cambios solicitados',scheduled:'Programada',reschedule_requested:'Reagenda solicitada',cancelled:'Cancelada',completed:'Completada',new:'Nueva',in_review:'En revisión'}
const navItems=[['summary','Resumen',LayoutDashboard],['brand','Perfil de marca',Palette],['deliverables','Entregables',Grid2X2],['requests','Solicitudes',MessageCircle],['calendar','Reuniones',CalendarDays],['billing','Facturación',Receipt],['strategy','Estrategia',Sparkles],['notifications','Notificaciones',Bell]]
const retainedClientCopy=['Resumen de tu cuenta','Lo que sigue','Entregables para revisión','Solicitudes recientes','Sobre tu marca','No hay pendientes visibles por ahora','client.summary','client.brand','client.requests','client.calendar','client.billing','client.strategy','client.notifications']

function initials(value='BI'){return value.split(/\s+/).filter(Boolean).map(word=>word[0]).join('').slice(0,2).toUpperCase()||'BI'}
function date(value){if(!value)return 'Sin fecha';const parsed=new Date(`${value}`.length===10?`${value}T12:00:00`:value);return Number.isNaN(parsed.getTime())?'Sin fecha':new Intl.DateTimeFormat('es',{dateStyle:'medium',timeStyle:`${value}`.includes('T')?'short':undefined}).format(parsed)}
function money(value,currency='USD'){return new Intl.NumberFormat('es',{style:'currency',currency}).format(Number(value||0))}
function LogoAvatar({brand,size='normal'}){return brand?.brand_logo_url?<img className={`brand-logo ${size}`} src={brand.brand_logo_url} alt={`Logo ${brand.brand_name||'marca'}`}/>:<div className={`brand-logo fallback ${size}`} aria-label={`Iniciales de ${brand?.brand_name||'marca'}`}>{initials(brand?.brand_name)}</div>}

export function AdminPreviewBar({clientName,onExit}){
  return <div className="client-admin-preview-bar"><div><span>Vista previa administrativa</span><strong>Estás viendo este portal como admin. Cliente: {clientName||'Sin nombre'}</strong></div><button onClick={onExit}><ArrowLeft size={16}/>Volver al panel admin</button></div>
}

export function ClientPortalLayout({brand,active,setActive,menu,setMenu,preview,onExitPreview,onSignOut,children}){
  return <div className={`client-portal-shell ${preview?'is-preview':''}`}>
    {preview&&<AdminPreviewBar clientName={brand?.brand_name} onExit={onExitPreview}/>}
    <div className="client-portal-frame">
      <aside className={menu?'open':''}>
        <header><strong>biem.</strong><button aria-label="Cerrar menú" onClick={()=>setMenu(false)}><X/></button></header>
        <div className="portal-brand-switch"><LogoAvatar brand={brand}/><div><small>PORTAL PRIVADO</small><strong>{brand?.brand_name||'Tu marca'}</strong></div></div>
        <nav>{navItems.map(([id,label,Icon])=><button className={active===id?'active':''} onClick={()=>{setActive(id);setMenu(false)}} key={id}><Icon size={18}/><span>{label}</span></button>)}</nav>
        {!preview&&<button className="portal-signout" onClick={onSignOut}><LogOut size={17}/>Cerrar sesión</button>}
      </aside>
      <main>
        <header className="portal-topbar"><button aria-label="Abrir menú" onClick={()=>setMenu(true)}><Menu/></button><div><small>BIEM DIGITAL</small><strong>{navItems.find(item=>item[0]===active)?.[1]}</strong></div><LogoAvatar brand={brand} size="small"/></header>
        <div className="client-portal-content">{children}</div>
      </main>
      {menu&&<div className="portal-overlay" onClick={()=>setMenu(false)}/>}
    </div>
  </div>
}

export function ClientAlertCard({icon:Icon=AlertTriangle,title,children,tone='warning'}){
  return <article className={`client-alert-card ${tone}`}><div className="client-alert-icon"><Icon size={20}/></div><div><h3>{title}</h3><p>{children}</p></div></article>
}

export function ClientPortalHeader({account,brand}){
  const active=account?.status==='active'
  return <section className="client-welcome-card"><LogoAvatar brand={brand} size="hero"/><div className="client-welcome-copy"><span className="client-eyebrow">RESUMEN DE TU CUENTA</span><h1>Hola, {account?.name||'bienvenido/a'}</h1><p>Todo el avance de <strong>{brand?.brand_name||account?.brand_name||'tu marca'}</strong> en un solo lugar.</p><div className="client-status-row"><span className={`client-status-badge ${active?'active':'neutral'}`}>{active?'Cliente activo':labels[account?.status]||'Portal privado'}</span><span>Portal privado</span></div></div><div className="client-welcome-brand"><small>MARCA</small><strong>{brand?.brand_name||account?.brand_name||'Tu marca'}</strong></div></section>
}

export function ClientSummaryCard({icon:Icon,title,value,description,onClick,actionLabel}){
  const content=<><div className="client-summary-icon"><Icon size={21}/></div><div className="client-summary-title">{title}</div><strong className="client-summary-value">{value}</strong><span className="client-summary-description">{description}</span>{actionLabel&&<span className="client-summary-action">{actionLabel}<ArrowRight size={14}/></span>}</>
  return onClick?<button className="client-summary-card is-clickable" onClick={onClick}>{content}</button>:<article className="client-summary-card">{content}</article>
}

export function ClientEmptyState({icon:Icon=CheckCircle2,title='No hay información disponible',text}){
  return <div className="client-empty-state"><div><Icon size={23}/></div><h3>{title}</h3>{text&&<p>{text}</p>}</div>
}

export function ClientBrandPreviewCard({brand,onOpen}){
  const incomplete=!brand?.brand_summary&&!brand?.industry
  return <section className="client-section-card brand-preview-card"><div className="brand-preview-identity"><LogoAvatar brand={brand} size="large"/><div><span className="client-eyebrow">PERFIL DE MARCA</span><h2>Sobre tu marca</h2><strong className="brand-preview-name">{brand?.brand_name||'Tu marca'}</strong><p className="brand-industry">{brand?.industry||'Industria por confirmar'}</p></div></div><div className="brand-preview-content"><p>{brand?.brand_summary||(incomplete?'Completa o confirma la información de tu marca para que podamos trabajar con datos actualizados.':'La información estratégica de tu marca está disponible en el perfil.')}</p><button className="client-secondary-button" onClick={onOpen}>Ver perfil de marca <ArrowRight size={15}/></button></div></section>
}

export default function ClientPortalApp({profile,onSignOut,previewData=null,onExitPreview}){
  const preview=Boolean(previewData)
  const [data,setData]=useState(previewData?normalizePreview(previewData):empty)
  const [loading,setLoading]=useState(!previewData),[error,setError]=useState(''),[notice,setNotice]=useState('')
  const [active,setActive]=useState('summary'),[menu,setMenu]=useState(false),[revision,setRevision]=useState(0),[modal,setModal]=useState('')
  useEffect(()=>{if(preview)return;let alive=true;setLoading(true);loadPortalWorkspace(profile).then(result=>{if(alive){setData(result);setError('')}}).catch(reason=>{if(alive)setError(reason.message)}).finally(()=>{if(alive)setLoading(false)});return()=>{alive=false}},[revision,profile.id])
  const mutate=async(operation,message)=>{setError('');setNotice('');try{await operation();setNotice(message);setModal('');if(!preview)setRevision(value=>value+1)}catch(reason){console.error('[BIEM client portal]',reason);setError(reason.message)}}
  const brand=data.brand||{brand_name:data.account?.brand_name}
  const panels={
    summary:<ClientDashboard data={data} brand={brand} setActive={setActive} mutate={mutate} preview={preview}/>,
    brand:<BrandProfile brand={brand} editable={!preview&&!data.account?.onboarding_completed} profile={profile} mutate={mutate} canDismissGuide={preview}/>,
    deliverables:<Deliverables items={data.deliverables} preview={preview} mutate={mutate} canDismissGuide={preview}/>,
    requests:<Requests items={data.requests} preview={preview} onCreate={()=>setModal('request')} canDismissGuide={preview}/>,
    calendar:<Calendar events={data.events} canDismissGuide={preview} preview={preview} mutate={mutate}/>,
    billing:<Billing invoices={data.invoices} canDismissGuide={preview}/>,
    strategy:<Strategy resources={data.resources} canDismissGuide={preview}/>,
    notifications:<Notifications preferences={data.preferences} canDismissGuide={preview} preview={preview} profile={profile} mutate={mutate}/>,
  }
  const agreement=previewData?.confidentiality?.active_agreement
  return <ClientPortalLayout brand={brand} active={active} setActive={setActive} menu={menu} setMenu={setMenu} preview={preview} onExitPreview={onExitPreview} onSignOut={onSignOut}>
    <div className="client-feedback-stack">{loading&&<div className="data-feedback loading">Cargando tu portal...</div>}{error&&<div className="data-feedback error"><AlertTriangle size={16}/>{error}</div>}{notice&&<div className="data-feedback success"><CheckCircle2 size={16}/>{notice}</div>}</div>
    {preview&&(agreement&&!previewData.confidentiality.accepted||!data.account?.onboarding_completed)&&<section className="client-alerts-stack">
      {agreement&&!previewData.confidentiality.accepted&&<ClientAlertCard title="Compromiso de confidencialidad pendiente">Este cliente todavía debe aceptar la versión {agreement.version} del compromiso de confidencialidad.</ClientAlertCard>}
      {!data.account?.onboarding_completed&&<ClientAlertCard icon={Clock3} title="Información pendiente" tone="info">El onboarding o confirmación de datos está pendiente: {data.account?.onboarding_type||'Sin clasificar'}.</ClientAlertCard>}
    </section>}
    {panels[active]}
    {modal==='request'&&<RequestModal onClose={()=>setModal('')} onSubmit={payload=>mutate(()=>createClientRequest(profile,payload),'Solicitud enviada. También recibirás confirmación por correo.')}/>}
  </ClientPortalLayout>
}

function normalizePreview(value){return {account:value.account,brand:value.brand,deliverables:value.deliverables||[],invoices:value.invoices||[],requests:value.requests||[],resources:value.resources||[],events:value.events||[],tasks:[],services:[],preferences:null}}

function statusGroup(status){if(['client_review','changes_requested','in_review'].includes(status))return 'review';if(['approved','scheduled'].includes(status))return 'scheduled';if(status==='published')return 'published';if(['pending','in_progress','internal_review'].includes(status))return 'progress';return 'neutral'}
function contentTypeLabel(value='Contenido'){const key=`${value}`.toLowerCase();if(key.includes('reel'))return 'Reel';if(key.includes('story')||key.includes('historia'))return 'Historia';if(key.includes('carrusel'))return 'Carrusel';if(key.includes('anuncio')||key.includes('ad'))return 'Anuncio';if(key.includes('email'))return 'Email';if(key.includes('blog'))return 'Blog';return key.includes('post')?'Post':value}
function platformLabel(item){const haystack=`${item.description||''} ${item.name||''}`.toLowerCase();if(haystack.includes('instagram'))return 'Instagram';if(haystack.includes('facebook'))return 'Facebook';if(haystack.includes('whatsapp'))return 'WhatsApp';if(haystack.includes('linkedin'))return 'LinkedIn';if(haystack.includes('tiktok'))return 'TikTok';return 'Por definir'}
function firstAsset(item){return item.file_url||item.drive_assets?.find(asset=>asset.is_primary)?.drive_url||item.drive_assets?.[0]?.drive_url||''}
function monthItems(items){const now=new Date();return items.filter(item=>{const value=item.scheduled_at||item.due_date||item.created_at;if(!value)return true;const parsed=new Date(`${value}`.length===10?`${value}T12:00:00`:value);return Number.isNaN(parsed.getTime())||parsed.getMonth()===now.getMonth()&&parsed.getFullYear()===now.getFullYear()})}
function progressLabel(done,total,label){return total>0?`${Math.min(done,total)} de ${total} ${label}`:`${done} ${label} registrados`}
function ClientDashboard({data,brand,setActive,mutate,preview}){
  const deliverables=monthItems(data.deliverables||[]), approvals=deliverables.filter(item=>['client_review','changes_requested'].includes(item.status)), published=deliverables.filter(item=>item.status==='published'), upcoming=deliverables.filter(item=>item.status!=='published').slice(0,6)
  const nextMeeting=(data.events||[]).filter(event=>new Date(event.start_time)>new Date()).sort((a,b)=>new Date(a.start_time)-new Date(b.start_time))[0]
  const lastMeeting=(data.events||[]).filter(event=>event.status==='completed'||new Date(event.start_time)<new Date()).sort((a,b)=>new Date(b.start_time)-new Date(a.start_time))[0]
  const pendingInvoice=(data.invoices||[]).find(item=>['pending','overdue'].includes(item.status))
  const activeRequests=(data.requests||[]).filter(item=>!['completed','rejected'].includes(item.status))
  const usedPosts=deliverables.filter(item=>['published','approved','scheduled'].includes(item.status)&&!/reel/i.test(item.content_type||'')).length
  const usedReels=deliverables.filter(item=>['published','approved','scheduled'].includes(item.status)&&/reel/i.test(item.content_type||'')).length
  const generalStatus=approvals.length?'Pendiente de aprobación':activeRequests.length?'En proceso':published.length?'Al día':'Requiere atención'
  const strategyResources=(data.resources||[]).filter(item=>['recommendation','diagnostic','growth_route','next_step'].includes(item.resource_type))
  const documents=[...(data.resources||[]).filter(item=>['brand_material','brief','recommendation','diagnostic'].includes(item.resource_type)&&item.file_url).map(item=>({id:item.id,name:item.title,type:item.resource_type,url:item.file_url,date:item.updated_at||item.created_at})),...deliverables.flatMap(item=>(item.drive_assets||[]).map(asset=>({id:asset.id,name:asset.name,type:asset.asset_type,url:asset.drive_url,date:item.updated_at||item.due_date})))]
  const approve=item=>mutate(()=>reviewClientDeliverable(item.id,'approved'),'Entregable aprobado.')
  const comment=item=>{const value=window.prompt('Comparte el ajuste o comentario para BIEM');if(value)mutate(()=>reviewClientDeliverable(item.id,'changes_requested',value),'Comentario enviado al equipo.')}
  return <div className="client-dashboard-page client-premium-dashboard">
    <section className="client-hero-panel"><div><span className="client-eyebrow">PORTAL DE CLIENTE</span><h1>{brand?.brand_name||data.account?.brand_name||'Tu marca'}</h1><p>Aquí puedes revisar el avance de tu estrategia, aprobar contenido y dar seguimiento a tu paquete.</p><div className="client-hero-actions"><button className="client-primary-button" onClick={()=>setActive('deliverables')}>Revisar contenido del mes</button><button className="client-secondary-button" onClick={()=>setActive('requests')}>Solicitar servicio adicional</button></div></div><aside><small>ESTADO DEL MES</small><strong>{generalStatus}</strong><span>{approvals.length?`${approvals.length} piezas esperan tu revisión`:published.length?`${published.length} publicaciones registradas`:'BIEM está preparando la información visible.'}</span></aside></section>
    <section className="client-overview-grid"><PackageContractCard account={data.account} invoice={pendingInvoice} usedPosts={usedPosts} usedReels={usedReels} meetings={(data.events||[]).length}/><MeetingPanel next={nextMeeting} last={lastMeeting} onOpen={()=>setActive('calendar')} onRequest={()=>setActive('requests')}/></section>
    <StrategyPanel resources={strategyResources}/>
    <section className="client-content-layout"><main><ContentMonth items={upcoming} onOpen={()=>setActive('deliverables')} onApprove={approve} onComment={comment} preview={preview}/><ApprovalPanel items={approvals} onApprove={approve} onComment={comment} preview={preview}/><PublishedPanel items={published} onOpen={()=>setActive('deliverables')}/></main><aside><ServicesPanel services={data.services||[]} onRequest={()=>setActive('requests')}/><DocumentsPanel documents={documents}/><ReviewNotesPanel hasComments={deliverables.some(item=>item.client_comments)}/></aside></section>
  </div>
}
function PackageContractCard({account,invoice,usedPosts,usedReels,meetings}){if(!account?.package_name)return <section className="client-section-card package-contract-card"><ClientEmptyState icon={Package} title="Todavía no tienes un paquete asignado." text="Cuando BIEM configure tu paquete, podrás verlo aquí."/></section>;const totalPosts=Number(account.graphic_pieces||0)+Number(account.carousels||0),totalReels=Number(account.reels||0);return <section className="client-section-card package-contract-card"><div className="client-section-heading compact"><div><span className="client-eyebrow">PAQUETE CONTRATADO</span><h2>{account.package_name}</h2><p>{account.package_description||'Servicios activos configurados para tu marca.'}</p></div><span className={`client-item-status ${account.status}`}>{labels[account.status]||account.status}</span></div><div className="package-contract-meta"><span>Renovación <strong>{date(account.renewal_date)}</strong></span><span>Próxima factura <strong>{invoice?`${money(invoice.amount,invoice.currency)} · ${labels[invoice.status]}`:'Sin factura pendiente'}</strong></span></div><div className="package-usage-list">{[[progressLabel(usedPosts,totalPosts,'publicaciones utilizadas'),totalPosts?usedPosts/totalPosts*100:0],[progressLabel(usedReels,totalReels,'reels completados'),totalReels?usedReels/totalReels*100:0],[`${Math.max(0,Number(account.meetings||0)-meetings)} reunión estratégica pendiente`,meetings&&account.meetings?meetings/account.meetings*100:0]].map(([label,value])=><div key={label}><div><span>{label}</span><b>{Math.round(value)}%</b></div><i><em style={{width:`${Math.min(100,Number(value)||0)}%`}}/></i></div>)}</div><div className="package-included-grid">{[['Posts mensuales',account.graphic_pieces],['Historias',account.stories],['Reels',account.reels],['Reporte mensual',account.includes_monthly_report?'Incluido':'No incluido'],['Reuniones',account.meetings],['Soporte',account.support_level||'Según paquete']].map(([label,value])=><article key={label}><small>{label}</small><strong>{value}</strong></article>)}</div></section>}
function StrategyPanel({resources}){const slots=[['Objetivo principal del mes','diagnostic'],['Campaña activa','growth_route'],['Mensaje clave','recommendation'],['Próxima acción estratégica','next_step']];return <section className="client-section-card strategy-premium-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">ESTRATEGIA EN CURSO</span><h2>Dirección estratégica visible</h2></div></div>{resources.length?<div className="strategy-mini-grid">{slots.map(([label,type])=>{const item=resources.find(resource=>resource.resource_type===type)||resources.shift?.();return <article key={label}><small>{label}</small><h3>{item?.title||'Pendiente'}</h3><p>{item?.content||'BIEM aún no ha cargado este bloque estratégico.'}</p>{item?.file_url&&<a href={item.file_url} target="_blank" rel="noopener noreferrer">Abrir recurso</a>}</article>})}</div>:<ClientEmptyState icon={Sparkles} title="BIEM aún no ha cargado la estrategia de este mes." text="Cuando esté disponible, podrás verla aquí. Para cubrir todos los campos se requiere una tabla mensual de estrategia con objetivo, campaña, mensaje, público, plataformas, enfoque, próxima acción y recomendaciones."/>}</section>}
function ContentPreview({item}){const asset=firstAsset(item);return <div className="content-preview">{asset&&/\.(png|jpe?g|webp|gif|svg)(\?|$)/i.test(asset)?<img src={asset} alt=""/>:/reel|video/i.test(item.content_type||'')?<Video size={24}/>:<Image size={24}/>}</div>}
function ContentMonth({items,onOpen,onApprove,onComment,preview}){return <section className="client-section-card content-month-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">CONTENIDO DEL MES</span><h2>Planificación y revisión</h2></div><button className="client-link-button" onClick={onOpen}>Ver todo <ArrowRight size={14}/></button></div>{items.length?<div className="content-gallery">{items.map(item=><article key={item.id}><ContentPreview item={item}/><div><span className={`client-item-status ${statusGroup(item.status)} ${item.status}`}>{labels[item.status]||item.status}</span><h3>{item.name}</h3><p>{contentTypeLabel(item.content_type)} · {platformLabel(item)}</p><small>{date(item.scheduled_at||item.due_date)}</small></div><footer><button onClick={onOpen}>Ver detalle</button>{!preview&&item.status==='client_review'&&<button onClick={()=>onApprove(item)}>Aprobar</button>} {!preview&&['client_review','changes_requested'].includes(item.status)&&<button onClick={()=>onComment(item)}>Comentar ajuste</button>}</footer></article>)}</div>:<ClientEmptyState icon={Grid2X2} title="Aún no hay contenido cargado para este mes." text="Cuando BIEM lo suba, podrás revisarlo aquí."/>}</section>}
function ApprovalPanel({items,onApprove,onComment,preview}){return <section className="client-section-card approval-focus-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">PENDIENTE DE APROBACIÓN</span><h2>{items.length?`${items.length} piezas esperan tu aprobación`:'Todo está al día'}</h2><p>{items.length?'Tu aprobación evita retrasos en las publicaciones de esta semana.':'No tienes piezas pendientes de aprobación.'}</p></div></div>{items.length?<div className="approval-list-premium">{items.map(item=><article key={item.id}><ContentPreview item={item}/><div><h3>{item.name}</h3><p>Fecha límite: {date(item.due_date||item.scheduled_at)}</p><small>{item.client_comments?'1 comentario visible':'Sin comentarios pendientes registrados'}</small></div><div>{!preview&&<button className="client-primary-button" onClick={()=>onApprove(item)}>Aprobar</button>}{!preview&&<button className="client-secondary-button" onClick={()=>onComment(item)}>Solicitar ajuste</button>}<button className="client-link-button" onClick={()=>onComment(item)}>Comentar</button></div></article>)}</div>:<ClientEmptyState icon={CheckCircle2} title="Todo está al día." text="No tienes piezas pendientes de aprobación."/>}</section>}
function PublishedPanel({items,onOpen}){return <section className="client-section-card published-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">PUBLICADO RECIENTEMENTE</span><h2>Contenido ya publicado</h2></div></div>{items.length?<div className="published-strip">{items.slice(0,4).map(item=><article key={item.id}><ContentPreview item={item}/><h3>{item.name}</h3><p>{contentTypeLabel(item.content_type)} · {platformLabel(item)}</p><small>{date(item.scheduled_at||item.updated_at)}</small>{item.publication_url?<a href={item.publication_url} target="_blank" rel="noopener noreferrer">Ver publicación</a>:<button onClick={onOpen}>Ver detalle</button>}</article>)}</div>:<ClientEmptyState icon={CheckCircle2} title="Aún no hay publicaciones registradas este mes."/>}</section>}
function MeetingPanel({next,last,onOpen,onRequest}){return <section className="client-section-card meeting-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">PRÓXIMA REUNIÓN</span><h2>{next?next.title:'No tienes reuniones programadas por ahora.'}</h2></div></div>{next?<><div className="meeting-date"><CalendarDays size={20}/><strong>{date(next.start_time)}</strong><span className={`client-item-status ${next.status}`}>{labels[next.status]||next.status}</span></div><p>{next.description||'Sesión de seguimiento con BIEM.'}</p><div className="meeting-actions"><button className="client-primary-button" onClick={onOpen}>Ver agenda</button>{next.google_meet_link&&<a href={next.google_meet_link} target="_blank" rel="noopener noreferrer">Entrar a la reunión</a>}</div></>:<button className="client-secondary-button" onClick={onRequest}>Solicitar reunión</button>}<div className="last-meeting"><small>Última reunión</small><strong>{last?date(last.start_time):'Sin reunión registrada'}</strong><p>{last?.description||'Los acuerdos y próximos pasos aparecerán aquí cuando BIEM los registre.'}</p></div></section>}
function ServicesPanel({services,onRequest}){return <section className="client-section-card services-catalog-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">SERVICIOS ADICIONALES</span><h2>¿Necesitas algo más?</h2></div></div>{services.length?<div className="service-catalog-grid">{services.slice(0,6).map(service=><article key={service.id}><Sparkles size={18}/><h3>{service.name}</h3><p>{service.description||'Servicio adicional disponible para tu marca.'}</p><strong>{Number(service.price_from)>0?money(service.price_from):'Cotizar'}</strong><small>{service.estimated_delivery||'Tiempo por confirmar'}</small><button onClick={onRequest}>Solicitar</button></article>)}</div>:<ClientEmptyState icon={Briefcase} title="BIEM aún no ha cargado servicios adicionales disponibles."/>}</section>}
function DocumentsPanel({documents}){return <section className="client-section-card documents-brand-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">DOCUMENTOS DE MARCA</span><h2>Archivos importantes</h2></div></div>{documents.length?<div className="document-card-list">{documents.slice(0,6).map(doc=><article key={doc.id}><FileText size={18}/><div><h3>{doc.name}</h3><p>{doc.type} · {date(doc.date)}</p></div><a href={doc.url} target="_blank" rel="noopener noreferrer">Ver</a></article>)}</div>:<ClientEmptyState icon={FileText} title="Todavía no hay documentos cargados para tu marca."/>}</section>}
function ReviewNotesPanel({hasComments}){return <section className="client-section-card review-notes-panel"><div className="client-section-heading compact"><div><span className="client-eyebrow">COMENTARIOS Y AJUSTES</span><h2>Revisión colaborativa</h2></div></div><p>{hasComments?'Tus comentarios visibles aparecen dentro de cada pieza de contenido.':'Aún no existe una conversación registrada para las piezas de este mes.'}</p><ClientEmptyState icon={MessageCircle} title="Historial avanzado de comentarios no disponible." text="Para mostrar hilos completos hace falta una tabla de comentarios por entregable con autor, mensaje, estado del ajuste y fecha."/></section>}
function DashboardDeliverables({items,onOpen}){return <section className="client-section-card"><div className="client-section-heading compact"><div><span className="client-eyebrow">REVISIÓN</span><h2>Entregables para revisión</h2></div><button className="client-link-button" onClick={onOpen}>Ver todos <ArrowRight size={14}/></button></div><div className="client-card-list">{items.slice(0,3).map(item=><article key={item.id}><div className="compact-list-icon"><Grid2X2 size={18}/></div><div><h3>{item.name}</h3><p>{item.content_type} · {date(item.due_date)}</p></div><span className="client-item-status client_review">Por revisar</span><button onClick={onOpen}>Ver entregable</button></article>)}</div></section>}
function DashboardRequests({items,onOpen}){return <section className="client-section-card"><div className="client-section-heading compact"><div><span className="client-eyebrow">SEGUIMIENTO</span><h2>Solicitudes recientes</h2></div><button className="client-link-button" onClick={onOpen}>Ver todas <ArrowRight size={14}/></button></div><div className="client-card-list">{items.map(item=><article key={item.id}><div className="compact-list-icon"><MessageCircle size={18}/></div><div><h3>{item.request_type}</h3><p>{date(item.created_at)}</p></div><span className={`client-item-status ${item.status}`}>{labels[item.status]||item.status}</span><button onClick={onOpen}>Ver solicitud</button></article>)}</div></section>}

function BrandProfile({brand,editable,profile,mutate,canDismissGuide}){const [editing,setEditing]=useState(false);const submit=event=>{event.preventDefault();const payload=Object.fromEntries(new FormData(event.currentTarget));mutate(()=>saveBrandBasics(payload),'Perfil de marca actualizado.');setEditing(false)};const upload=async event=>{const file=event.target.files?.[0];if(!file)return;const url=await uploadBrandLogo(profile,file);await mutate(()=>saveBrandBasics({brand_logo_url:url}),'Logo actualizado.')};return <div className="brand-profile-page"><div className="brand-cover" style={brand?.brand_cover_image_url?{backgroundImage:`url(${brand.brand_cover_image_url})`}:undefined}><LogoAvatar brand={brand} size="hero"/><div><span>PERFIL DE MARCA</span><h1>{brand?.brand_name||'Tu marca'}</h1><p>{brand?.industry||'Industria por completar'}</p></div>{editable&&<button onClick={()=>setEditing(!editing)}>Editar datos básicos</button>}</div>{!brand?.brand_about&&!brand?.brand_summary&&<GuideExample guideKey="client.brand" canDismiss={canDismissGuide}/>} {editing?<form className="brand-edit-form" onSubmit={submit}><input name="brand_name" value={brand?.brand_name||''} placeholder="Nombre de marca"/><input name="industry" value={brand?.industry||''} placeholder="Industria"/><textarea name="brand_summary" value={brand?.brand_summary||''} placeholder="Descripción corta"/><textarea name="brand_about" value={brand?.brand_about||''} placeholder="Sobre la marca"/><input name="website_url" value={brand?.website_url||''} placeholder="Sitio web"/><input name="instagram_url" value={brand?.instagram_url||''} placeholder="Instagram"/><input name="whatsapp_number" value={brand?.whatsapp_number||''} placeholder="WhatsApp"/><input type="file" accept="image/*" onChange={upload}/><button className="admin-primary">Guardar cambios</button></form>:<><section className="brand-about-card"><span>SOBRE LA MARCA</span><h2>Sobre {brand?.brand_name||'tu marca'}</h2><p>{brand?.brand_about||brand?.brand_summary||`${brand?.brand_name||'Esta marca'} está construyendo una presencia clara y consistente junto a Biem Digital.`}</p></section><div className="brand-detail-grid">{[['Qué ofrece',brand?.main_products_services],['Público objetivo',brand?.target_audience],['Propuesta de valor',brand?.value_proposition],['Tono de comunicación',brand?.communication_tone],['Tipografías',brand?.typography],['Ubicación',brand?.location]].map(([title,value])=><article key={title}><span>{title}</span><p>{value||'Por completar'}</p></article>)}</div><section className="client-section-card"><div className="client-section-heading compact"><div><span className="client-eyebrow">ENLACES PRINCIPALES</span><h2>Presencia digital</h2></div></div><div className="brand-links">{[['Sitio web',brand?.website_url],['Instagram',brand?.instagram_url],['Facebook',brand?.facebook_url],['TikTok',brand?.tiktok_url],['WhatsApp',brand?.whatsapp_number]].filter(([,value])=>value).map(([label,value])=><a href={value.startsWith?.('http')?value:`https://wa.me/${value}`} target="_blank" rel="noopener noreferrer" key={label}>{label}</a>)}</div></section></>}</div>}

function PageHeading({eyebrow,title,copy,action}){return <div className="client-page-heading"><div><span className="client-eyebrow">{eyebrow}</span><h1>{title}</h1>{copy&&<p>{copy}</p>}</div>{action}</div>}
function Deliverables({items,preview,mutate,canDismissGuide}){return <section className="client-section-card"><PageHeading eyebrow="CONTENIDO" title="Entregables" copy="Revisa el contenido preparado para tu marca."/>{items.length?<div className="client-deliverable-list">{items.map(item=><article key={item.id}><div className="deliverable-main"><div className="compact-list-icon"><Grid2X2 size={19}/></div><div><h3>{item.name}</h3><p>{item.content_type} · {date(item.due_date)}</p>{item.client_comments&&<p className="deliverable-comment">{item.client_comments}</p>}{item.drive_assets?.length>0&&<div className="portal-drive-assets"><span>ARCHIVOS EN GOOGLE DRIVE</span>{item.drive_assets.map(asset=><a href={asset.drive_url} target="_blank" rel="noopener noreferrer" key={asset.id}><FileText size={15}/><div><strong>{asset.name}</strong><small>{asset.asset_type}{asset.is_primary?' · Principal':''}</small></div><ArrowRight size={14}/></a>)}</div>}</div></div><div className="deliverable-actions"><span className={`client-item-status ${item.status}`}>{labels[item.status]||item.status}</span>{!preview&&item.status==='client_review'&&<><button className="client-primary-button" onClick={()=>mutate(()=>reviewClientDeliverable(item.id,'approved'),'Entregable aprobado.')}>Aprobar</button><button className="client-secondary-button" onClick={()=>{const comment=window.prompt('Indica los cambios solicitados');if(comment)mutate(()=>reviewClientDeliverable(item.id,'changes_requested',comment),'Cambios solicitados.')}}>Solicitar cambios</button></>}</div></article>)}</div>:<GuideExample guideKey="client.deliverables" canDismiss={canDismissGuide}/>}</section>}
function Requests({items,preview,onCreate,canDismissGuide}){return <section className="client-section-card"><PageHeading eyebrow="SEGUIMIENTO" title="Solicitudes" copy="Consulta tus solicitudes y las respuestas del equipo." action={!preview&&<button className="client-primary-button" onClick={onCreate}><Plus size={16}/>Nueva solicitud</button>}/>{items.length?<div className="client-card-list">{items.map(item=><article key={item.id}><div className="compact-list-icon"><MessageCircle size={18}/></div><div><h3>{item.request_type}</h3><p>{date(item.created_at)} · {item.description}</p>{item.admin_response&&<p className="portal-response">Biem: {item.admin_response}</p>}</div><span className={`client-item-status ${item.status}`}>{labels[item.status]||item.status}</span></article>)}</div>:<GuideExample guideKey="client.requests" canDismiss={canDismissGuide}/>}</section>}
function Calendar({events,preview,mutate,canDismissGuide}){return <section className="client-section-card"><PageHeading eyebrow="AGENDA" title="Próximas reuniones" copy="Fechas y enlaces de tus próximas sesiones con Biem."/>{events.length?<div className="client-event-list">{events.map(event=><article key={event.id}><div className="event-date"><CalendarDays/><strong>{date(event.start_time)}</strong></div><div><h3>{event.title}</h3><p>{event.description||'Reunión de seguimiento'}</p><small>{event.location||'Reunión online'} · {event.timezone}</small></div><div className="event-actions">{event.google_meet_link&&<a href={event.google_meet_link} target="_blank" rel="noopener noreferrer">Entrar a Google Meet</a>}{!preview&&event.status==='scheduled'&&<button onClick={()=>{const note=window.prompt('¿Qué fecha u horario prefieres?');if(note)mutate(()=>requestEventReschedule(event.id,note),'Solicitud de reagenda enviada.')}}>Solicitar reagenda</button>}<span className={`client-item-status ${event.status}`}>{labels[event.status]||event.status}</span></div></article>)}</div>:<GuideExample guideKey="client.calendar" canDismiss={canDismissGuide}/>}</section>}
function Billing({invoices,canDismissGuide}){return <section className="client-section-card"><PageHeading eyebrow="CUENTA" title="Facturación" copy="Consulta tus facturas y estados de pago."/>{invoices.length?<div className="client-card-list">{invoices.map(item=><article key={item.id}><div className="compact-list-icon"><CreditCard size={18}/></div><div><h3>{item.invoice_number}</h3><p>Vence {date(item.due_date)}</p></div><strong className="invoice-amount">{money(item.amount,item.currency)}</strong><span className={`client-item-status ${item.status}`}>{labels[item.status]||item.status}</span></article>)}</div>:<GuideExample guideKey="client.billing" canDismiss={canDismissGuide}/>}</section>}
function Strategy({resources,canDismissGuide}){return <section className="client-section-card"><PageHeading eyebrow="ESTRATEGIA" title="Recomendaciones y diagnóstico" copy="Decisiones y próximos enfoques visibles para tu marca."/>{resources.length?<div className="strategy-grid">{resources.map(item=><article className="strategy-card" key={item.id}><span>{item.resource_type}</span><h3>{item.title}</h3><p>{item.content}</p>{item.file_url&&<a href={item.file_url} target="_blank" rel="noopener noreferrer">Abrir recurso</a>}</article>)}</div>:<GuideExample guideKey="client.strategy" canDismiss={canDismissGuide}/>}</section>}
function Notifications({preferences,preview,profile,mutate,canDismissGuide}){const values=preferences||{email_enabled:true,request_updates:true,deliverable_updates:true,invoice_updates:true,recommendation_updates:true,calendar_updates:true};const submit=event=>{event.preventDefault();const form=new FormData(event.currentTarget);const payload={};['email_enabled','request_updates','deliverable_updates','invoice_updates','recommendation_updates','calendar_updates'].forEach(key=>payload[key]=form.get(key)==='on');mutate(()=>saveNotificationPreferences(profile,payload),'Preferencias guardadas.')};return <section className="client-section-card"><PageHeading eyebrow="COMUNICACIÓN" title="Notificaciones por correo" copy="Elige qué actualizaciones deseas recibir."/>{!preferences&&<GuideExample guideKey="client.notifications" canDismiss={canDismissGuide}/>}<form className="notification-form" onSubmit={submit}>{[['email_enabled','Recibir correos del portal'],['request_updates','Actualizaciones de solicitudes'],['deliverable_updates','Entregables listos'],['invoice_updates','Facturas y recordatorios'],['recommendation_updates','Recomendaciones estratégicas'],['calendar_updates','Reuniones y calendario']].map(([key,label])=><label key={key}><input type="checkbox" name={key} checked={values[key]}/><span>{label}</span></label>)}{!preview&&<button className="client-primary-button">Guardar preferencias</button>}</form></section>}
function RequestModal({onClose,onSubmit}){const submit=event=>{event.preventDefault();const values=Object.fromEntries(new FormData(event.currentTarget));onSubmit({...values,priority:'medium'})};return <div className="modal-backdrop" onMouseDown={onClose}><form className="portal-modal" onSubmit={submit} onMouseDown={event=>event.stopPropagation()}><header><h2>Nueva solicitud</h2><button type="button" onClick={onClose}><X/></button></header><label>Título<input name="request_type" required/></label><label>Descripción<textarea name="description" required/></label><label>Fecha deseada<input name="desired_due_date" type="date"/></label><footer><button type="button" onClick={onClose}>Cancelar</button><button className="client-primary-button"><Send/>Enviar solicitud</button></footer></form></div>}
